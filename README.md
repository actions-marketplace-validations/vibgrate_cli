# cli
The Vibgrate CLI scans your codebase for upgrade drift — the gap between the dependency versions you run and the versions you should run. It produces a deterministic Upgrade Drift Score (0–100) and actionable findings to help you maintain a healthy, secure codebase.
